/// <reference path="../../../typings/dojo/dojo.d.ts" />

/* 
 * Mendix Quirck, we need to re-require in order to start loading the definition
 * the require must be place after the module, after compilation 
 */
require(["ProgressBarButton/widget/ProgressBarButton"]);